apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: inference-workspace-requester
rules:
  - apiGroups:
      - inference.redhat.com
    resources:
      - inferenceworkspaces
    verbs:
      - create
