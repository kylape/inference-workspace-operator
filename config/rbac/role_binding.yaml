apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: inference-workspace-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: inference-workspace-operator
subjects:
  - kind: ServiceAccount
    name: inference-workspace-operator
    namespace: inference-workspace-operator
